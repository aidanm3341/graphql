type Query {
    positions(symbolFilter: String): [Position]
}

type Mutation {
    addTrade(symbol: String, longshort: String, qty: Int, price: Float): Trade
}

type Position {
    symbol: String
    trades: [Trade]
}

type Trade {
    symbol: String
    longshort: String
    qty: Int
    price: Float
}